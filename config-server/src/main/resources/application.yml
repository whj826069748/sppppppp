eureka:
    client:
        serviceUrl:
            defaultZone: http://localhost:1001/eureka/
server:
    port: 7001
spring:
    application:
        name: config-server
    cloud:
        config:
            server:
                git:
                    uri: https://github.com/whj826069748/sppppppp
                    searchPaths: config-center
                    username: username
                    password: password
